
x Two by two optimal solutions
x Rewrite func separate routes to be recursive
- goroutines to find separates faster. Or not.
x low averages
x reduce optimals:
    x remove subsets
    x remove functionally similar (same lengths)